"use strict";

class User {
  constructor(firstName, lastName, userName, password, newsPerPage, category) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.userName = userName;
    this.password = password;
    // this.newsPerPage;
    // this.category;
  }
}
